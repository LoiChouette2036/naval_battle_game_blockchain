<h1>All Games</h1>
<%= link_to "Create a New Game", new_game_path %>

<ul>
  <% @games.each do |game| %>
    <li>
      <%= link_to "Game ##{game.id}", game_path(game) %> 
      - Bet Amount: <%= game.bet_amount %> 
      - Status: <%= game.status %>
    </li>
  <% end %>
</ul>